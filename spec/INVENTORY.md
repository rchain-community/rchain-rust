# RChain invariant inventory

The catalog of mathematical invariants the Rust rewrite must preserve. Each row links a law to its
source-of-truth location, its Lean formalization, and its Rust type realization; a human-facing
walkthrough of each law → the concrete Rust file/type/function that carries it (and the test that
gates it) is in [`../docs/src/contributor/laws-to-rust.md`](../docs/src/contributor/laws-to-rust.md).

**Type system.** The port's own type discipline — the ρ-calculus embedded as the base sort of a
Calculus of Constructions, with no silent partiality — is specified in [`TYPE-SYSTEM.md`](TYPE-SYSTEM.md),
guided by `Rchain/Rho.lean` and `Rchain/Ty.lean`. It is **not** a new law and does not pre-empt Laws
1–29 below.

**Concurrency model.** How Laws 1, 2, 4, 7, 8, 9, 10, 11, 19 combine to allow concurrent reduction — and
what must serialize, and why — is specified in
[`../docs/src/formal/concurrency-model.md`](../docs/src/formal/concurrency-model.md). Its soundness
theorems (diamond/linearization, disjoint-commute, sharded-scheduler, replay determinism) are the target
of `Rchain/Concurrent.lean`. **Effect-level correction:** the "disjoint-channel" reading of Law 9 is
*insufficient* — sound concurrent *effect* scheduling requires disjoint **continuation closures**, not
disjoint footprints; the channel-sharded scheduler is therefore unsound (see `Rchain/Effect.lean`, the
proved counterexample `effect_reorder_diverges`, and `docs/src/formal/effect-scheduling.md` S.3/S.4).
Without further discipline the reducer's only sound parallelism is Level 1 (pure-resolution fork-join).
**Scheduler expansion (Laws 20–22)** lifts that ceiling: a per-channel claim queue that commits ops in
DFS path order (Law 20, "1 channel = 1 logical task"), the sequential-equivalent gate scheduler (Law 21),
and dispatch-time closure computability (Law 22) are the only sound concurrent effect schedulers — see
`Rchain/Scheduler.lean` and
[`../docs/src/formal/channel-scheduler.md`](../docs/src/formal/channel-scheduler.md).
The **on-chain extension (Laws 23–25)** makes the relaxed interleaving sound for the block path by
**validated speculation**: commits may reorder iff each validates DFS-order serializability (Law 24's
versioned write-record layer with prefix visibility), invalidated subtrees abort and re-run under the
gate, so the published log is the sequential fold's — see `Rchain/SchedulerOnchain.lean` and
[`../docs/src/formal/onchain-scheduling.md`](../docs/src/formal/onchain-scheduling.md).
The **cross-shard extension (Laws 26–29)** lifts that discipline from one shard to many: shard scope
is deterministic (Law 26, a typed `ShardId`), and a cross-shard transaction is a **two-phase commit** —
a Git-pull-request flow (propose → stage → review → merge, or close) whose coordinator commits on
every participant or aborts on every one (Law 27), with idempotent legs (Law 28) and a durable,
content-addressed decision record (Law 29) — see `Rchain/CrossShard.lean` and
[`../docs/src/formal/cross-shard-transactions.md`](../docs/src/formal/cross-shard-transactions.md).

**Status legend**

`spec/LAWS.md` and `spec/laws.tsv` are the *checked* rendering of the same rows (emitted from
`Rchain/Laws.lean` and refused stale by the gate); these cells are prose, and where the two disagree the
register is right.

- **proven** — proved in `spec/Rchain/`, over the model; the register distinguishes `proved-tied`
  (proved *and* tied to the node by a conformance corpus) from `proved-model`
- **owed** — the model exists and the proof does not (or, where a note says so, the statement is not yet
  the code's and a counterexample is proved instead)
- **vacuous** — proved, but the statement restates its own definition and cannot fail
- **stated** — theorem statement exists in Lean (Phase 0), proof is a later obligation
- **Phase N** — to be formalized and proven in phase N (1 = execution core, 2 = RSpace, 3 = Rosette,
  4 = Casper, 5 = crypto/storage)
- **axiom** — postulated (cryptographic primitive), not proven, by design
- **residual axiom** — a proof obligation currently declared `axiom` (not by design), e.g. the
  comparator lawfulness in Law 1 (the 12 the register counts: `cmpExpr`'s `eq_iff` and `swap`, and
  the ten element `lt_trans` laws)
- **open** — recorded question, not yet formalizable

## Laws

| # | Layer | Law | Source of truth | Rust type realization | Lean | Status |
|---|-------|-----|-----------------|----------------------|------|--------|
| 1 | Rholang | `Par`/`ESet`/`EMap` are commutative; canonicalization to a total order is **idempotent** (`sort(sort p) = sort p`); `sort(p\|q) = sort(q\|p)` | `models/src/main/scala/coop/rchain/models/rholang/sorter/ScoreTree.scala`, `ordering.scala`, `models/src/main/scala/coop/rchain/models/SortedParHashSet.scala` | `Sorted<Par<S>>` (canonical `Eq`/`Ord`/`Hash`/`Serialize`) + `sorter::sort_par` | `Rchain/Sort.lean` (`sortPar_idempotent`, `sortPar_comm`) | **proven** (`sortPar_idempotent` + `sortPar_comm` + leaf laws + `sortList` are proven); residual: the element-comparator lawfulness (**12 axioms**, not the 30 this row used to claim: `cmpExpr`'s `eq_iff` and `swap`, and the ten element `lt_trans` laws — the 12 list-comparator laws and `cmpGUnforgeable_lt_trans` are discharged; counted by `Rchain/Laws.lean`, the register, whose rows cite exactly the tree's axioms) is the remaining "total order" obligation · `spec/conformance/sort.tsv` ← `Rchain/Corpus.lean` (`sortCases_decide`, 19 pairwise verdicts, all three of `lt`/`eq`/`gt`) · `rholang/tests/lean_sort_corpus.rs` (reads each verdict back from **which element `sort_par` puts first**, the only place the order is observable — the canonical types are the quotient by the comparator, so a sorted-spelling corpus would be vacuous) — **and its first run found a divergence, now closed**: the model ordered by *declaration* order, the node by its **score tree**, and the two differed across eleven types. Four structures were reordered to the tags and are pinned by rows 13–19, each verdict observed from the node first (a send's field order — `persistent` first; the par's own field order — `exprs` before `news`; the expression-class order; and `GBool`'s polarity, `true` first), each row falsified by restoring the old order before being believed. The rest is a recorded boundary, not a fixed defect: the model's algebra is coarser (21 `Expr` constructors against the node's 33). It was an *order* gap, never a fork between nodes, and the detail is in law 1a's note and `Rchain/Sort.lean`'s comparator note |
| 2 | Rholang | **α/name equivalence** = par order + `\| Nil` + top-level arithmetic + α + added eval/quote | `rholang/src/main/k/rholang/name-equivalence.k:1-14`, `rholang/reference_doc/normalization_process/README.md` | `Par<S>` structural equality (`≡` = sorted `Par` + `quote`/`eval`) | `Rchain/Sort.lean` (≡ core in `Rho.lean`; deep α is Coq `Laws.v`) | **stated** (≡ core proven in `Rho.lean`) |
| 3 | Rholang | **Capture-avoiding de Bruijn substitution**; `sort(subst t) = subst(sort t)`, and closedness is preserved **given a closed image** | `rholang/src/main/scala/coop/rchain/rholang/interpreter/Substitute.scala`, `Env.scala` | `rholang::substitute::substitute_par` (total on `Closed`) | `Rchain/Subst.lean` (+ Coq `Laws.v`) | **owed** — the three axioms are postulates, not a definition, and as stated they **constrained nothing**: `noSubst` (the function that substitutes nothing) satisfies both laws (`the_identity_satisfies_sort_subst`, `the_identity_satisfies_subst_closed`). The closedness law also **lacked its hypothesis** — `Closed` is "no `Var.free`" and `σ` is arbitrary, so an open image at a bound occurrence escapes (see the register row's `bound_is_closed_free_is_not`); the Rust's own test carries `arb_closed`, so the statement is narrowed to `∀ v, Closed (σ v) → …`. The boundary is pinned from the code side by `an_open_value_at_the_variable_leaves_a_free_variable` (`rholang/src/property_tests.rs`), and the row closes when Programme D's unit 5 mirrors `substitute_par` in the model |
| 4 | Rholang | **Reduction (comm)**; first-match-wins; `new` yields fresh unforgeable names | `rholang/.../interpreter/Reduce.scala`, `rholang/src/main/k/rholang/*.k` | `rholang::reduce::DebruijnInterpreter` + `Tuplespace` (`Reduce`) | `Rchain/Reduce.lean` (COMM core in `Rho.lean`; `reduce_freeVars_subset` **proven** against `FreeVars.lean`'s definition) | **proven** (COMM core in `Rho.lean`; single-step determinism is *disproved* as a universal — `reduce_not_deterministic` — and the freshness half is `reduce_freeVars_subset`, no longer an axiom) |
| 5 | Rholang | **Spatial matching**; a free var is bound at most once (`addedVars.distinct`) | `rholang/.../interpreter/matcher/SpatialMatcher.scala`, `ParCount.scala` | `rholang::matcher::spatial_match` + `free_count: FreeCount` fields on `ReceiveBind`/`MatchCase` | `Rchain/Match.lean` — `spatialMatch*` **defined** (the `ETuple` arm added: AUDIT C44), `spatialMatch_implies_linear` **proven**; `concrete_matches_iff_eq` (now over `modelledPar`, because its old domain admitted an arithmetic pattern) and `fuel_saturation` owed | **defined + partly proven** — it was three `axiom`s, one of them *false* (AUDIT C26); the false one is replaced by the linearity the implementations actually enforce. **Where the enforcement is**: the *normalizer* refuses a twice-bound binder in both contexts (`normalizer.rs:111,289,590,1325`), measured on a devnet (AUDIT C42), so the matcher's own two paths are unreachable from a term |
| 6 | Rholang | **No globally free variables** in a program | `rholang/src/main/k/rholang/{free,program-restrictions}.k`, `models/.../HasLocallyFree.scala` | `models::types::Closed` (newtype) | `Rchain/FreeVars.lean` — `freeVarOf` **defined** and `closed_iff_no_freeVars` **proven**; the `Closed` checker is `Ty.lean`'s | **proven** — both halves used to be `axiom`s (an opaque predicate plus the tie to the checker), which the row recorded as "a defined-but-undefined-elsewhere predicate"; the predicate is a `mutual` block mirroring `closed*` and the tie is an induction |
| 7 | RSpace | **Join commutativity** (channel keys hashed in sorted order) | `rspace/.../hashing/StableHashProvider.scala:18-22` | `rspace::hashing::StableHashProvider::hash_seq` (sorted) | `Rchain/RSpace/Join.lean` — `joinKey` **defined** as hash-of-sorted-channel-hashes, `joinKey_perm` **proven** from law 1's `sortList_perm` | **proven** |
| 8 | RSpace | **Deterministic COMM** (candidate selection sorted-first by content hash; produce refs sorted; content-addressed events) | `rspace/.../trace/Event.scala:35-39`, `rspace/.../SpaceMatcher.scala` | `rspace::space_matcher` (sorted candidates) + `rspace::rspace` (sorted produce) + `Comm` event | `Rchain/RSpace/Comm.lean` — `Produce`/`Consume`/`Comm`/`produceRefs`/`commId` **defined** over the Rust's own sort, `comm_content_addressed` **proven** | **proven** |
| 9 | RSpace | **Merge is a monoid**; non-conflicting logs commute — *strengthened for effect scheduling*: disjoint **closure** (not footprint) ⇒ commute | `rspace/.../merger/{StateChange,ChannelChange,EventLogMergingLogic}.scala` | `rspace::merger::state_change_merger` (`compute_trie_actions`) | `Rchain/RSpace/Merge.lean`; `Rchain/Effect.lean` (`effect_reorder_diverges` proved; `effect_commute_of_disjoint_closure` **proven** via locality) | **proven** — `mergeChanges`/`NonConflicting` defined (added/removed concatenate, the join map is right-biased overwrite, `state_change.rs:186-189`), `mergeChanges_assoc`/`_comm` **proven**; the strengthened effect-level commute `effect_commute_of_disjoint_closure` was already proven |
| 10 | RSpace | **Merkle determinism**: content-addressed radix trie, collision-free **on well-formed nodes**, empty-root | `rspace/.../history/RadixTree.scala:50-68` | `rspace::history::RadixTreeImpl` (`Node = [Item; 256]`) | `Rchain/RSpace/Merkle.lean` — `Item`/`Node`/`encodeNode`/`nodeHash`/`emptyNode`/`emptyRoot` **defined** (the encoder now mirrors `radix_tree.rs:48-77`), `WellFormed` names the invariant the code carries in its types, `root_collision_free`/`nodeHash_eq_emptyRoot` **proven given `WellFormed`** | **owed** — the canonicity axiom was **false as stated**: it was claimed over `Node := List Item` and `Hash := List Byte`, both wider than the code's `[Item; 256]` and `Hash32([u8; 32])`, and `the_encoder_is_not_canonical_over_the_models_types` exhibits two distinct nodes with the same 37-byte encoding (a one-item node whose value is 35 bytes, and a two-item node). The code cannot produce either — the types and the ≤32-byte prefixes (inside the 7-bit length field) exclude the second reading — so the statement was narrowed to `WellFormed` instead of the model widened. The narrowed canonicity's proof is owed |
| 11 | RSpace | **Replay determinism**: recomputed COMM ⊆ recorded trace | `rspace/.../ReplayRSpace.scala:68-71` | `rspace::ReplayRSpace` | `Rchain/RSpace/Comm.lean` — **vacuous**: in-model "recomputed" and "recorded" would be the same function, so the claim would be `rfl`; the port checks membership **both** ways (`replay_rspace.rs:330-332`, `:580-590`) | **vacuous** (re-statement owed: it needs a model of the recorded store) |
| 12 | Rosette | **Actor atomicity** (single-threaded `mbox.nextMsg`) | `rosette/README:27-35`, `roscala/.../ob/Actor.scala:52-61` | *deferred* (`rosette`/`roscala` orphaned) | *(none)* | **orphaned** (Rosette VM out of scope) |
| 13 | Rosette | **Reflection**: everything is an `Ob`; meta/parent chain; **fork-join barrier** | `roscala/.../ob/{Ob,Meta,Ctxt}.scala`, `Vm.scala:185-188` | *deferred* | *(none)* | **orphaned** (Rosette VM out of scope) |
| 14 | Casper | **Finality requires > 2/3** bonded stake; fringe = one message per bonded validator (antichain) | `sdk/.../consensus/Stake.scala:8`, `block-storage/.../dag/Finalizer.scala:76,133` | `sdk::consensus::is_super_majority` (exact `3·stake > 2·total`) + `BTreeMap<S, NonNegI64>` bonds | `Rchain/Casper/{Stake,Fringe}.lean` — `calculateFringe` is the port's `calculate_fringe` (full-partition filter, non-bonded skip, exact-integer comparison) and `finality_iff_supermajority` is the advance gate, **proven** with the 2/3, 2⁵³ and `i64` boundary theorems; `fringe_antichain` was **false of a bare `Fringe`** (refuted in the tree, `fringe_antichain_is_false`) | **proven** (the gate) / **owed** (the antichain, which needs the finalizer's derivation) |
| 15 | Casper | Fringe **monotone by height**; **seen-set monotone** (no regression) | `block-storage/.../dag/MessageMapSyntax.scala:33`, `casper/.../Validate.scala:285` | `BlockHeight`/`SeqNum` in `block-storage::dag::Message` | `Rchain/Casper/Fringe.lean` — both axioms were **false of a bare `Fringe`/`Message`** and are refuted in the tree; the port *constructs* the seen set (`seenOf`, `seenOf_contains_justifications`/`mem_seenOf_self` **proven**) | **owed** — the transitive closure needs a DAG model this layer lacks |
| 16 | Casper | **Block number** = max(parent)+1; **seqNum** strictly +1; **content addressing** (`hash = Blake2b256(block−{hash,sig})`); **bonds cache = PoS state** | `casper/.../Validate.scala:177,249,256,360`, `util/ProtoUtil.scala:70` | `BlockHeight`/`SeqNum` + `BlockHash`/`StateHash` (over `Hash32`) | `Rchain/Casper/Validate.lean` — the two number laws are the port's own checks (`BlockNumberValid`/`SeqNumValid`, folds over the justifications seeded `-1`), **proven** as their elimination; both old universal forms were **false** (`block_number_universal_is_false`, `seq_num_universal_is_false`); `blockHash`/`content_addressing` **proven** from `blake2b256_collision_free` + the serializer's canonicity | **proven** (16d, the bonds cache, is still open) |
| 17 | Casper | **Merge determinism** (unique min-cost rejection); numeric channels non-negative/no-overflow; RNG merge commutative | `sdk/.../dag/merging/ConflictResolutionLogic.scala:200`, `rholang/.../merging/RholangMergingLogic.scala:90` | `NonNegI64` bonds and heights — **not** numeric channels, which are signed `i64` with ordinary negative diffs; `Blake2b512Random` merge | `Rchain/Merging.lean` — `checkedAdd`/`checkedSub` with their refusal witnesses, `mergeRandoms_perm`; the unchecked accumulator beside them is AUDIT C41 | **proven** (the checked arithmetic and the call-site canonicalization) |
| 18 | Storage | **Height map contiguous** (no holes); **fringe identity order-independent** | `block-storage/.../BlockMetadataStore.scala:156`, `models/.../FringeData.scala:32` | `BlockHeight` + `block-storage::dag::metadata_store::validate_dag_state` | `Rchain/Casper/Validate.lean` — `Contiguous` is the store's invariant with `contiguous_insert_succ`/`contiguous_skip_leaves_hole` **proven**, and `fringeId`/`fringeId_perm` prove the order-independence the `BTreeSet` supplies; the two axioms were **false as stated** and are refuted in the tree | **proven** |
| 19 | Crypto | Blake2b256 canonical hash with a **fixed 32-byte output**; the `Blake2b512Random` merge is **n-ary and order-sensitive** (no algebraic law of it holds); sig verify/sign; Curve25519 round-trip | `crypto/.../` (`Blake2b512Random`, `Secp256k1`, `Curve25519`) | `Blake2b256Hash` (over `Hash32`) + `Blake2b512Random` (**axiom**) | `Rchain/Crypto/{Random,Spec}.lean` — `Msg`/`Hash` are byte strings, so the width is `blake2b256_output_is_32_bytes`, and collision-freedom is documented as collision-*resistance* stated as injectivity | **axiom** (by design) |
| 20 | Scheduler | **Channel-task linearization** ("1 channel = 1 logical task"): ops on a channel commit in DFS path order through a per-channel claim queue; at most one op executes per channel at any instant | this spec (`Rchain/Scheduler.lean`) + `docs/src/formal/channel-scheduler.md` | `rspace::concurrent::channel_queue::ChannelClaimQueue` (path-ordered claims, head-on-all commit) | `Rchain/Scheduler.lean` (`queue_commit_path_ordered` **proven**, whose statement no longer carries an unused sortedness hypothesis; `pathSorted_head_minimal` — the bakery argument's core: a path-sorted queue's head is its path-smallest element) | **proven** (the ordering and the head-is-smallest core). The *global* liveness claim the old `law20_deadlock_freedom` axiom made is **not provable as stated** — it quantified over every `Chan → Queue`, and `PathLt` is not well-founded on paths (`[0] > [0,0] > [0,0,0] > …`), so an unbounded pending set need have no minimum; the corrected statement carries the finiteness the real system has. The Rust's own liveness evidence is stress tests plus its deferral of cross-channel cycles (`rspace/src/concurrent/channel_queue.rs:24-28`, risk R3) |
| 21 | Scheduler | **DFS-gate linearization**: the gate scheduler (op at DFS path `p` runs only after all ops at paths `< p` complete) refines sequential `Effect.apply`; one-hop next-step pruning is unsound (depth-2 counterexample) | this spec (`Rchain/Scheduler.lean`) + `docs/src/formal/channel-scheduler.md` | `rholang::scheduler::EffectMode::Gate` (task `i` holds task `i−1`'s handle and awaits it first — a linear chain, not the quadratic join) | `Rchain/Scheduler.lean` (`gate_await_closure_orders` — the immediate-predecessor chain is *transitively* complete, which is what makes the linear chain sufficient; `one_hop_depth2_diverges`) | **proven** (the dependency structure and the counterexample). The old `gate_exec_refines_apply` defined `gateApply` as the sequential fold and then proved the fold is the fold; the Rust's own comment says the gate is "not a speedup — the sound, sequential-equivalent carrier" (`reduce.rs:2324-2327`), so it is gone |
| 22 | Scheduler | **Next-step closure is computable at dispatch** (matched data is concrete); `effect_commute_of_disjoint_closure` promoted axiom → **theorem** (locality induction) | `rholang/.../interpreter/Reduce.scala` (continuation dispatch) + this spec | dispatch-time footprint via `rholang::reduce::resolve_children`, which takes **no store** — that signature is the purity | `Rchain/Scheduler.lean` (`depth2_next_step_disjoint` **proven**) + `Rchain/Effect.lean` | **the negative half is proven; the positive half is a signature fact, not a theorem.** `resolve_children`'s type has no store parameter, so stating "it does not read the tuple space" as a Lean theorem would prove that a function ignores an argument nobody passes; the old `next_step_closure_computable` was `by rfl` and is deleted. **And the tempting distributivity law is false**: resolving `p \| q` term-wise is *not* `resolve p ++ resolve q`, because the Rust flattens all sends before all receives — found by trying to prove it. See `spec/laws.tsv` row 22 (`vacuous`) |

| 23 | Scheduler | **Read-determinism**: an effect's chosen candidate, commit outcome, and event trace are a deterministic function of the state it reads (two states agreeing on the effect's closure yield identical traces) | this spec (`Rchain/SchedulerOnchain.lean`) | content-addressed selection (`rspace/src/rspace.rs` sorted-first matching) | `Rchain/SchedulerOnchain.lean` (`read_state_determines_outcome`) | **proven** |
| 24 | Scheduler | **DFS-order serializability**: a concurrent execution is sound for the block path iff every commit read exactly the state the DFS-earlier effects produced — the versioned write-record layer (`SpecState := Chan → Option (DfsPath × Nat × Bool)`) witnesses reads that Bool values alone cannot (the depth-2 stale read); the S.3 B-first interleaving and the C/D later-write pollution both fail prefix visibility, the DFS order itself validates; the writer chain (`serializable_writer_chain`, path-nodup + initial-record hypotheses, each forced by a decided boundary witness) and the pinned publication theorem (`pinned_run_publication`: dispatched + path-nodup + per-channel path-pinned commit order ⇒ the commit-order fold reaches the gate fold's state and each commit emits the gate's trace; the converse is disproved by the produce-only witness) are proven | this spec (`Rchain/SchedulerOnchain.lean`) + `docs/src/formal/onchain-scheduling.md` | block-path acceptance gate of `rholang::scheduler::EffectMode::RelaxedValidated` (validation certificate) | `Rchain/SchedulerOnchain.lean` (`prefixVisible`/`ValidCommit`/`DFSSerializable`, witnesses proven; `record_determines_value`/`dispatched_record_at` proven; `serializable_writer_chain` + `pinned_run_publication` proven) | **proven** (certificate positive content + pinned publication; the certificate's blind spot — `certificate_blind_late_writer_diverges` — keeps the oracle backstop load-bearing). The publication theorem carries **no** certificate hypothesis: it was named `dfs_serializable_implies_log_equal` and carried an unused `DFSSerializable` argument, hidden by `set_option linter.unusedVariables false`; both are gone, renamed to state what it proves |
| 25 | Scheduler | **Validated speculation**: a scheduler may commit effects in any order iff each commit validates Law 24; invalidated runs fall back to the whole-run gate re-run, so the published state is the sequential fold's — certificate fail-fast + oracle accept + fallback re-run (`published`) | this spec (`Rchain/SchedulerOnchain.lean`) + `docs/src/formal/onchain-scheduling.md` | block-path validated-relaxed mode (speculative run + certificate + gate re-run fallback), whose publication rule is `oracle_hash == relaxed_hash && logs_match`, else the oracle's result | `Rchain/SchedulerOnchain.lean` (`published` + `published_state_is_the_oracles` **proven**, `Published` (path-nodup coordinator invariant) + `fallback_rerun_published` proven) | **proven** (the publication *rule*: accept only on agreement with the oracle, else ship the oracle's result — so the certificate decides how much is re-run, never what is published). `validated_speculation_refines_apply` (a disjunction whose second arm held for any run) and `gate_replay_terminates` (`⟨f x, rfl⟩` — totality of a Lean function) are deleted |
| 26 | Cross-shard | **Shard scope determinism**: a deploy/block's effects bind to exactly one shard; the shard id is a validated, ordered value; the RNG seed + unforgeable names are shard-scoped | this spec (`Rchain/CrossShard.lean`) + `docs/src/formal/cross-shard-transactions.md` | the `ShardId` newtype + `parent-shard-id` hierarchy (`shared/src/refined.rs`, `ShardSpec`/`ShardMemberships` in `casper/src/conf.rs` — a node's memberships are a non-empty, duplicate-free list); shard-scoped seed/unforgeable names (`casper/src/block_random_seed.rs`); deploy admission + block validation shard checks (`casper/src/api/block_api_impl.rs`, `casper/src/validate.rs`); per-shard storage isolation + routing (`casper/src/storage.rs`, `node/src/api/shard_routing.rs`) | `Rchain/CrossShard.lean` — the axiom (`∀ l, ValidShardId l.shard`) was **FALSE** over a freely-constructible `Leg` and is refuted (`shard_scope_deterministic_is_false`, witness `Leg.mk "" 0 0`); the law's content is the gateway's ingress validation (`node/src/web/http.rs:203-218`), which needs that function modelled | **owed** (was `stated`; a false axiom is worse than an owed proof) |
| 27 | Cross-shard | **Cross-shard atomicity (2PC)**: a transaction commits on every participant or aborts on every participant — no run leaves a strict subset committed | this spec (`Rchain/CrossShard.lean`) + `docs/src/formal/cross-shard-transactions.md` | the `rho:txn` participant (`rholang/src/system_processes.rs`) + the client-side `TxnCoordinator::run_2pc` (`casper/src/txn_coordinator.rs`) over the `casper/src/shard_invoke.rs` remote-deploy primitive, and the on-node gateway coordinator for a node that is a member of the shards involved (`casper/src/gateway/`, `POST /api/v1/txn`) | `Rchain/CrossShard.lean` — the axiom (`∀ r : Run, uniform r`) was **FALSE** over `Run := List Outcome` and is refuted (`txn_atomic_is_false`, witness `[committed, aborted]`); the narrowed statement is `run_2pc`'s — one decision, applied to every leg that prepared (`casper/src/txn_coordinator.rs:152-192`) | **owed** (was `stated`) |
| 28 | Cross-shard | **Leg idempotency**: `prepare`/`commit`/`abort` are idempotent under the transaction id (re-delivery and client retry are safe) | this spec (`Rchain/CrossShard.lean`) + `docs/src/formal/cross-shard-transactions.md` | `txn_prepare`/`txn_commit`/`txn_abort` idempotent under `txn_id` (`rholang/src/native_state.rs`) | `Rchain/CrossShard.lean` — `leg_idempotent` **proven**, plus `txnPrepare`/`txnCommit`/`txnAbort` modelled on the ledger the port writes, with the three idempotence theorems, the fences (`commit_after_abort_is_an_error`, `abort_after_commit_is_an_error`) and `prepare_refuses_overdraft` | **proven** |
| 29 | Cross-shard | **Decision durability & record determinism**: the coordinator's decision is a durable, content-addressed record; a prepared participant can always recover it; the merge/close record is re-derivable on replay | this spec (`Rchain/CrossShard.lean`) + `docs/src/formal/cross-shard-transactions.md` | the `PREFIX_TXN` native ledger (`rholang/src/native_state.rs`, `rspace/src/native_store.rs`) for the participants, and the node-local coordinator ledger (`casper/src/gateway/ledger.rs`, `<data-dir>/gateway/`) with boot-time recovery — durable but deliberately *not* consensus state, since a coordinator's votes belong to one node; content-addressing analog of Laws 11/16 | `Rchain/CrossShard.lean` — the axiom was **FALSE** over a freely-constructible `CoordRecord` and is refuted (`commit_record_deterministic_is_false`); **the decision half is proved** (`allReady`/`coordinatorDecision` are the port's own lines, `coordinator_decision_committed_iff`), the durability half stays owed | **owed** (was `stated`) |

## Laws 30-43: the surface the ten silent defects live in

Ten defects of one class — nothing errors, the branch simply never runs — have been found on a node and
patched one at a time (AUDIT C9, C10, C13, C16-C22, and the two the instrument itself found: C23's model
gap and C24's parser hole). The 29 laws above are about the calculus; the incidents happened in the
*surface the matcher reads*. These rows append that surface to the catalog. Rows 30-33 are the grammar
and the printer, 34-36 the normalizer, 37-38 the matcher, 39-41 reply and protocol shapes, 42-43 the
JSON envelope.

Each row, like the 29, ends in a check: a Lean declaration, a corpus emitted from it
(`spec/conformance/`, generated by `lake exe rchain-corpus`, committed, and refused stale by
`tools/check-lean-conformance.sh`), and a Rust consumer that parses the same source and must agree 1:1.

| # | Layer | Law | Lean | Corpus + Rust consumer | Status |
|---|-------|-----|------|------------------------|--------|
| 30 | Rholang | Every term the parser accepts is in the BNFC grammar (`rholang_mercury.cf`) | *(pending)* `Syntax` | *(pending)* | **open** — `parser.rs` is more permissive in places (AUDIT C24's residual) |
| 31 | Rholang | Every BNFC term is accepted, modulo a data list of documented deviations — **the list has two rows**: the *comma* form of a collection remainder (`[head, ...tail]`) and a **trailing separator** (`[1, 2,]`, `contract c(@x,)`), each accepted because the shipped contracts and the Scala-era fixtures use it and both are equally underivable by the grammar (AUDIT C31) | *(pending)* `Syntax` | *(pending)* | **open** (the parser's side is `parser.rs`'s `a_trailing_separator_is_accepted_as_a_deviation`; the Lean side is `Rchain/Parse.lean`, which does not exist) |
| 32 | Rholang | Lexical determinism: comments, the `_`/`_ident` rule, `bundle0`, number forms, and the operator spellings (`/\`, `\/`, `...`, `++`, `--`, `<<-`, `<=`) each lex one way | `Rchain/Lex.lean` — the *operator* surface as data (`lexemes`), with `lexemes_decide`: spellings distinct and punctuation, and **maximal munch** (`longestMatchIn`) — each row's spelling is its own longest match, so a table where `<` shadowed `<=` fails the `<=` row; each row carries a *sample* term and what it must observe | `spec/conformance/lex.tsv` ← `Rchain/Corpus.lean` (18 rows) · `node/tests/lean_lex_corpus.rs`, which runs each sample and reports (`value:<json>` through law 42's JSON, or `answers:<n>`) | **checked for the operator surface** — the rest of the law (comments, the `_`/`_ident` rule, `bundle0`, the literal forms) needs the full lexer laws 30/31/33 share, and is named as the boundary; C10's swap would now fail a sample |
| 33 | Rholang | `parse (print p) ≡ p` on `Par` (the C13 round-trip, stated) | *(pending)* `Syntax` | *(pending)* | **open** |
| 34 | Rholang | A *value* position (condition, target, datum, element, pattern, name) is normalized against an **empty** `par`; only a statement continuation inherits | `Rchain/Surface.lean` (`normalizeAt`); `Rchain/Corpus.lean`'s `c21Cases`/`c21Holds` (`decide`d through `cmpPar`, and `c21IsProbe` so a case that is its own condition fails) | `spec/conformance/c21.tsv` ← `Rchain/Corpus.lean` (`c21Cases_decide`, 5 cases) · `rholang/tests/lean_c21_corpus.rs` | **checked** on the shapes AUDIT C21 broke — the register's `proved-tied`. The universal form is the named boundary: the model threads no accumulator, so the rule is true of it by construction and the corpus ties the *node* |
| 35 | Rholang | `connective_used` is sound: it holds iff the term contains a connective, free var, wildcard **or remainder**, per collection form | `Rchain/Par.lean` (`connectiveUsed`, `Expr.remainder`, `Var.isConnective`), `Sort.cmpOptionVar`, `Ty.closedRemainder` | `spec/conformance/flags.tsv` ← `Rchain/Corpus.lean` (`flagCases_decide`, 17 cases) · `rholang/tests/lean_normalize_corpus.rs` | **checked** (model `decide`d; Rust agrees 1:1; found AUDIT C24) |
| 36 | Rholang | The normalizer's output is well-scoped and closed (law 6 through every path) | *(pending)* `Normalize` | *(pending)* | **open** |
| 37 | Rholang | Match soundness and completeness (law 5 strengthened: partial collections, wildcards, remainders) | `Rchain/Match.lean` (`spatialMatchCore`/`spatialMatchExprs`/`spatialMatchExpr`/`matchListPar`/`matchMap` over `Par`, with fuel so `decide` can reduce them; `concrete_matches_iff_eq` owed, over `modelledPar` — the domain its own tuple/arithmetic cases forced, AUDIT C44) | `spec/conformance/match.tsv` ← `Rchain/Corpus.lean` (`matchCases_decide`, 17 cases, verdicts three-valued: `true`/`false`/`rejected`) · `rholang/tests/lean_match_corpus.rs` (its own runtime per case, control datum) | **checked** (model `decide`d; Rust agrees 1:1; the once-false law-5 axiom replaced — AUDIT C26) |
| 38 | Rholang | Silence is specified: an unmatched receive or `match` yields no reduction **and no error** | `Rchain/Silence.lean` — `ReduceP` (the rules that consume a datum carry the match *and* the channel name: `comm` for one datum and `commPs` for a call's arity — the clause AUDIT C40 found missing — and both now take the receive's `freeCount`/`bindCount` as parameters, because the port's `free_count` is `count_no_wildcards` and the canonical values the constructors used to fix could not derive what the search accepts: AUDIT C45), `takesStep`, `receiveParP`; **`takesStep_sound` is proved**, and `takesStep_iff_reduces` (scoped to `allStringChans` by C40) is the remaining axiom — its statement was **false** as first written (`chan = nilPar` refutes it) | `spec/conformance/silence.tsv` ← `Rchain/Corpus.lean` (`silenceCases_decide`, 13 cases — case 13 is the join the search used to claim as a step) · `rholang/tests/lean_silence_corpus.rs` | **checked** (the rule is the law; the sound direction is proved, the complete direction is owed and now correctly stated, and C40/C45 record both defects) |
| 39 | Protocol | Every `rho:*` urn's reply arity/shape equals its `spec/API-SCHEMA.md` row | `Rchain/Protocol.lean` — `replyCatalog` (the rows as *data*), with `replyCatalog_decide`: namespaced unique urns, the reply kind agreeing with its slots, and the declared arity agreeing with the arguments as written (C22 item 2's class) | `spec/conformance/protocol.tsv` ← `Rchain/Corpus.lean` (9 rows from `replyCatalog`) · `rholang/tests/lean_protocol_corpus.rs` (calls each urn with the row's own arguments, then classifies every slot of the reply) · the doc tie: `tools/check-lean-conformance.sh` requires a `spec/API-SCHEMA.md` row per catalog urn | **checked for the rows the surface language can spell** — the crypto, `deployerId:ops` and `authToken:ops` urns take a `ByteArray`, which the grammar has no literal for, so their shapes stay pinned by `system_process_conformance.rs`'s hand-written probes and the catalog grows as rows that can be spelled arrive |
| 40 | Protocol | Every call in the protocol catalog has an accepting receive at the target's arity | `Rchain/Silence.lean` — `stepsInBinds` reads the *arity* (as many patterns as data; `receiveParPs` is the multi-pattern receive a `contract` head becomes), so a call at the wrong arity has **no step** by the rule, not by a remark | `spec/conformance/silence.tsv` cases 7-12 ← `Corpus.silenceCases_decide` (law 38's layer: the verdict needed *is* `takesStep`, and a second consumer would be a copy) · `rholang/tests/lean_silence_corpus.rs` | **checked** (C22 item 2's check: case 10 is `write!(key, value)` against `write(@key, @value, ret)`) |
| 41 | Protocol | Channel balance: a replicable reader restores what it consumes | `Rchain/Store.lean` — `replicatedRead` (the reader a `contract` installs), `readStore` (one read, computed), `storeSurvives` (the law: a datum back *and* law 38's `takesStep`) | `spec/conformance/store.tsv` ← `Rchain/Corpus.lean` (`storeCases_decide`, 5 cases) · `rholang/tests/lean_store_corpus.rs` · the installed content: `casper/tests/genesis_registry.rs`'s `a_read_does_not_destroy_the_inbox`, and the contract that was unreachable — `a_fresh_chain_answers_one_group_creation` / `..._two_group_creations_in_one_deploy` (AUDIT C25: `Group`'s restore at `:65` was **measured**, so the `:49`-`:65` window is latency, not loss) | **checked** (C22 item 1's check; the static walk over the vendored text is deliberately not shipped — see AUDIT §17 C22) |
| 42 | JSON | `rho_expr_to_par (expr_from_par p) = p`, and the `0 → absent`, `1 → unwrapped`, `n → ExprPar` envelope rule | `Rchain/Json.lean` — `parToJE` (the envelope: `envelope` over the three fields `RhoExpr` reads), `jeToPar`, `render` (the wire text `serde` writes); `decode_encode` **proved** over `flatPar`, whose unforgeable arm had to be closed first — the old predicate admitted `par [unforg, unforg]`, which decodes to `Nil` and encodes to nothing (`unforgPair_refutes_the_old_statement`) | `spec/conformance/json.tsv` ← `Rchain/Corpus.lean` (`jsonCases_decide`, 12 cases) · `node/tests/lean_json_corpus.rs` (each case: the node's own `expr_from_par`, then its own `rho_expr_to_par` round-trip) | **proved** — the round trip is a theorem over the corrected domain, *and* checked at the wire level (`jsonCases_decide` + the node's own codec in `lean_json_corpus.rs`); the unforgeable leaf is outside the model's domain (its `GUnforgeable` carries a level, not the wire bytes) and stays pinned by `rho_expr.rs`'s unit tests |
| 43 | JSON | Each endpoint's serialized shape equals the schema's (camelCase fields, `[]` semantics, error precedence) | `Rchain/Envelope.lean` — `envelopeCatalog` (the envelopes as data) with `envelopeCatalog_decide`: **no key contains an underscore** (C16's rule), keys distinct, union tags capitalized, names unique | `spec/conformance/envelope.tsv` ← `Rchain/Corpus.lean` (`envelopeCaseCount`, 6 rows) · `node/tests/lean_envelope_corpus.rs`, which holds *both* parties to the catalog — the DTOs' serialization and the served `OPENAPI_JSON` document | **checked** for the envelope's keys and tags; the *types* behind them and the document's coverage are named as the boundary (AUDIT C29 fixed the two rows the check found stale) |

## Laws 44-47: the Proof-of-Stake epoch

The rows above are about the *language* and the *surface*. These four are about the validator
lifecycle's **clock**, and they exist because the port had the lifecycle and no epoch: a membership
change took effect the moment its deploy ran, and a reward law had nothing to be about. The oracle is
the blessed contract `legacy/casper/src/main/resources/Pos.rhox` (the port's native replacement is
`rholang/src/native_state.rs`), and the model is `Rchain/Pos.lean` — which models the epoch's
**arithmetic** (the pot and the split) and not its state machine, so two of the four rows are `open`
in the register's vocabulary with their Rust evidence named rather than an over-claimed status.

| # | Layer | Law | Lean | Corpus + Rust consumer | Status |
|---|---|---|---|---|---|
| 44 | PoS | **The epoch gate**: membership takes effect at an epoch boundary (`blockNumber % epochLength = 0`); off one, a bond is pooled but not activated, a withdrawal is staged but not moved, and no claim is paid | `Pos.rhox:517-519` (`closeBlock`'s gate) + `rholang/src/native_state.rs` (`close_block`, `is_epoch_boundary`) | — no Lean model of the transition: `Rchain/Pos.lean` has `rewardPot`/`reward`, the arithmetic, and the gate is Rust-side | **open** — implemented and pinned, in `rholang/src/native_state.rs`, by `the_epoch_gate_does_nothing_off_a_boundary` (it builds the off-boundary state with a staged withdrawal *and* a full pot, asserts the whole state is unchanged, then that the same call at the boundary moves and pays it), `bond_escrows_the_stake_and_activates_at_the_boundary` and `withdraw_stages_the_validator_until_the_next_boundary` |
| 45 | PoS | **The epoch reward split**: `pot * (bondᵢ / minimumBond) / (activeBonds / minimumBond)` per active validator, out of `pot = posBalance − totalBond − totalWithdraw − committedRewards`, committed per validator and paid only when the validator leaves | `Pos.rhox:241-256` (`getCurrentEpochRewards`), `:568-576` (`commitCurrentEpochRewards`) | `Rchain/Pos.lean` — `rewardPot`, `reward` (the formula as a definition) | **proved over the model** — the tie to the Rust is a *boundary*: a named test rather than a mechanism, `an_epoch_splits_the_pot_and_keeps_the_dust` (`rholang/src/native_state.rs`) builds the model's own case — minimum bond 3, bonds 4 and 5, a pot of 10 paid in as phlo — and reads the split back. Where the contract is undefined (`minimumBond` 0, or a normaliser of 0) it divides by zero and faults the deploy; the port pays zero (`spec/AUDIT.md` §6) |
| 46 | PoS | **The split does not conserve**: `Σ rewards ≤ pot`, and the difference is the dust of two integer divisions — which is not lost but stays in the pot for the next epoch | `Pos.rhox:247-249` (the two divisions) | `Rchain/Pos.lean` — `sum_rewards_le_pot`, `list_sum_div_le`, `div_add_div_le`, `nsum_map_mul_left`, `the_dust_is_real` | **proved over the model**, with a strict instance `decide`d: `the_dust_is_real` is minimum bond 3, bonds `[4, 5]`, pot 10 — six units distributed of ten. A conservation law written as an *equality* would be false, and `Pos.rhox`'s own comment does not say which it means. The tie is a *boundary*: `an_epoch_splits_the_pot_and_keeps_the_dust` asserts the same six-of-ten against the Rust and that the four units of dust stay in its pot |
| 47 | PoS | **Withdrawal staging**: the request records `quarantineLength + epochLength * (1 + blockNumber / epochLength)` and changes nothing else; the validator leaves the pool at the next boundary and is paid `bond + committed rewards` at the first boundary past its quarantine | `Pos.rhox:363-387` (the staging), `:577-587` (the move out of the pool), `:592-621` (the payout) + `rholang/src/native_state.rs` (`withdraw`, `close_block`) | — no Lean model of the transition | **open** — pinned, in `rholang/src/native_state.rs`, by `withdraw_stages_the_validator_until_the_next_boundary` (the three stages asserted separately: still bonded and active with a deadline; out of the pool and escrowed at the first boundary; paid at the second) and `a_released_withdrawal_pays_the_bond_plus_the_committed_rewards` (the sum `bond 40 + committed 5 = 45` **and** that the staking vault is exactly emptied by it) |

## Law 48: the fee consequence of a denied deploy

RCHIP-02 says a *denied* deploy — one the block merge excludes for a conflict or a dependency — should
cost its deployer nothing and reward its validator nothing. Neither this port nor the Scala it was
ported from implements that, and the row exists so the gap is a stated rule rather than a paragraph: a
denied deploy's effects are excluded from the merged state, and its cost still counts in the objective
the merge optimizes.

| # | Layer | Law | Lean | Corpus + Rust consumer | Status |
|---|---|---|---|---|---|
| 48 | Casper | **A denied deploy's fee consequence**: its effects are excluded from the merged state, while its cost still counts toward the merge's objective — so RCHIP-02's "the deployer is not charged, the validator is not rewarded" holds in neither tree | `MergeScope.scala:87` (`rejectionCost = DeployChainIndex.deployChainCost`) and `DeployChainIndex.scala:73` (`.map(_.cost).sum`) — the Scala does not implement it either | — no Lean model, and no corpus: the row states a rule neither tree implements, and the objective it is about is a `Long` sum over the denied set (`casper/src/merging.rs`'s `deploy_chain_cost`, tested by `deploy_chain_cost_sums`) | **open**, and a decision: the close chosen is to record it as an open design question shared with the Scala. Implementing it means choosing an objective *and* a refund path the Scala does not have, which is a consensus change with no oracle to check it against. `docs/src/node/block-merge.md`'s open-items list carries the long form, including what Programme B changed about it: the deployer's phlo used to be burned (so "the validator is not rewarded" was true for want of any reward) and now funds the epoch pot (laws 45/46), where the active set receives it — at epoch granularity, with no per-deploy counterpart in which "the validator is not rewarded *for this deploy*" could be stated |

| 49 | Rholang | **What a matched produce or consume is charged**: the storage is charged up front and what the match consumed is **refunded** — the continuation's consume storage and the produce storage of every removed datum — *before* the event and COMM costs | `ChargingRSpace.scala:105-127` (`handleResult`'s `Some` arm, `refundForConsume`, `refundForRemovingProduces`) + `rholang/src/storage.rs` (`refund_storage`, `consume_id`) | `Rchain/Charging.lean` — `prefixes`, `peak`, `itotal`, `peak_four`, `peak_refunds_first`, `itotal_refunds_first` | **proven over the model** — the tie to the Rust is a *boundary*: `a_matched_produce_refunds_its_storage_before_the_event_costs` (`rholang/src/storage.rs`) asserts the total (which fails if a refund is dropped) *and* the order, by finding a balance between the two peaks at which the deploy completes and one phlo below which it does not. `peak_refunds_first` is the arithmetic of that second half, and `itotal_refunds_first` is why the total alone cannot see it |

## Open questions

1. `casper/src/main/resources/casper.tla` models only the genesis **bootstrap ceremony handshake**.
   The finality rule is now formalized in `CasperFinality.tla` (Laws 14/15/16: > 2/3 supermajority,
   fringe antichain + seen-set monotonicity, seqNum strictly increasing), reconstructed from
   `Finalizer.scala` + `MessageMapSyntax.scala`.
2. The `faultTolerance` field asserted in `integration-tests/test/test_dag_correctness.py:105-111`
   is not computed anywhere in this tree — its exact formula is declared an open question;
   `CasperFinality.tla` states the safety margin (`3·support − 2·total`) and the monotonicity the
   test relies on.
3. `Rosette` (`rosette/`, `roscala/`) is not wired into `build.sbt` and is **orphaned** — Laws 12–13
   are documented (no Lean files); the Rust reducer (`rholang::reduce`) replaces the VM, so these two
   laws are out of scope for the formalization.
