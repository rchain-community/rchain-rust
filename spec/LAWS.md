43 laws, 53 entries: 6 proved and tied to the node by a conformance corpus, 9 proved over the model, 0 proved but vacuous (the statement restates its own definition), 1 axiomatized by design (the cryptographic primitives), 11 owed, 15 deferred, 9 open, 2 orphaned.

### Rholang

| Law | Invariant | Status | Lean | Rests on | Tied by | Models | Falsified by |
|---|---|---|---|---|---|---|---|
| **1a** | `Par`/`ESet`/`EMap` are commutative and canonicalization is idempotent and commutative: `sort (sort p) = sort p`, `sort (p \| q) = sort (q \| p)` <br/> <em>`sortPar_idempotent` is proved only *for* a comparator whose element laws hold; the element-law half is clause b, and it is axioms</em> | proved-model | Rchain.sortPar_idempotent, Rchain.sortPar_comm, Rchain.sortPar, Rchain.parMerge | - | - | models/src/sorter.rs | `sortPar_idempotent`/`sortPar_comm` are theorems; `spec/INVENTORY.md`'s Law 1 claim of idempotence is falsified by any leaf type whose comparator is not a total order — see clause b, where exactly that is assumed rather than proved |
| **1b** | Each element comparator (`cmpPar`, `cmpSend`, …, `cmpConnective`) is a lawful total order: `eq_iff`, `swap`, `lt_trans` <br/> <em>12 axioms, not 30: `Sort.lean`'s own header says 33 and `spec/INVENTORY.md` says 30, and both are stale. The list comparators' laws were discharged by induction on the list</em> | owed | Rchain.cmpPar, Rchain.cmpSend, Rchain.cmpExpr | Rchain.cmpExpr_eq_iff, Rchain.cmpExpr_swap, Rchain.cmpPar_lt_trans, Rchain.cmpSend_lt_trans, Rchain.cmpReceiveBind_lt_trans, Rchain.cmpReceive_lt_trans, Rchain.cmpNew_lt_trans, Rchain.cmpMatchCase_lt_trans, Rchain.cmpMatch_lt_trans, Rchain.cmpExpr_lt_trans, Rchain.cmpBundle_lt_trans, Rchain.cmpConnective_lt_trans | - | - | `cmpGUnforgeable_lt_trans` is proved while its ten siblings are axioms, and `cmpListSend_lt_trans` is proved from them — so a counterexample to any element law would be a counterexample to those proofs. No witness is published for the axioms themselves, which is the gap: nothing would notice if one of them were false |
| **2** | α/name equivalence = par order + `\| Nil` + top-level arithmetic + α + added eval/quote <br/> <em>the deep α half is Coq's obligation (`spec/coq/Laws.v`), where it is an `Axiom`</em> | proved-model | Rchain.StrCong, Rchain.strCong_equivalence, Rchain.strCong_comm, Rchain.strCong_assoc, Rchain.strCong_ident, Rchain.strCong_nil_left | - | - | models/src/ast.rs | `reduce_not_deterministic` (`Rchain/Concurrent.lean`) exhibits two distinct reductions of one term, which is what makes `≡` — rather than syntactic identity — the relation reduction needs |
| **3** | Capture-avoiding de Bruijn substitution; `sort (subst t) = subst (sort t)` <br/> <em>`substPar` is an `axiom`, so the two laws are statements about an undefined function; the metatheory lives in Coq</em> | deferred | Rchain.substPar, Rchain.sort_subst, Rchain.subst_closed | Rchain.substPar, Rchain.sort_subst, Rchain.subst_closed | - | - | *owed* |
| **4a** | Reduction (COMM): a send and a matching receive on one channel reduce to the receive's body | proved-model | Rchain.Reduce, Rchain.reduce_closed, Rchain.reduce_not_deterministic | - | - | rholang/src/reduce.rs | `reduce_not_deterministic` proves confluence is **false** on the flat `Par`, so the law's statement is bounded by a published disproof rather than an assertion |
| **4b** | `new` yields fresh unforgeable names: reduction introduces no free variables it did not already have | owed | Rchain.reduce_freeVars_subset | Rchain.reduce_freeVars_subset | - | - | *owed* |
| **5** | Spatial matching; a free variable is bound at most once <br/> <em>`spatialMatch_implies_linear` is `h.2` of a conjunct inside `spatialMatch`'s own definition, so it holds by construction: it is not yet a statement about the matcher's clauses</em> | owed | Rchain.spatialMatch, Rchain.spatialMatch_implies_linear, Rchain.spatialMatchCore | Rchain.concrete_matches_iff_eq, Rchain.fuel_saturation | match | - | the corpus's three-valued verdicts (`true`/`false`/`rejected`) include the rejected case a twice-bound pattern produces — the shape the previous law-5 axiom *denied* and which `spec/conformance/match.tsv` now pins (AUDIT C26) |
| **6** | No globally free variables in a program <br/> <em>`Closed` itself is proved; the tie to `freeVarOf` is the axiom, which is what makes the law rest on a defined-but-undefined-elsewhere predicate</em> | owed | Rchain.Closed, Rchain.closed, Rchain.Closed_parMerge_iff, Rchain.Closed_receivePar_iff, Rchain.closed_anyPat | Rchain.freeVarOf, Rchain.closed_iff_no_freeVars | - | - | `Closed` is a `decide`d `Bool` checker with a proved agreement lemma (`closed_eq_Closed`), so both directions of the predicate are testable on concrete terms |
| **30** | Every term the parser accepts is in the BNFC grammar (`rholang_mercury.cf`) <br/> <em>`parser.rs` is more permissive in places (AUDIT C24's residual); needs `Rchain/Parse.lean` and the grammar as data. The gate already reserves a `parse` corpus consumer (`tools/check-lean-conformance.sh:132`) with nothing behind it yet</em> | open | - | - | - | - | *owed* |
| **31** | Every BNFC term is accepted, modulo a data list of documented deviations | open | - | - | - | - | *owed* |
| **32** | Lexical determinism: comments, the `_`/`_ident` rule, `bundle0`, number forms and the operator spellings each lex one way <br/> <em>checked for the *operator* surface only; comments, `_`/`_ident`, `bundle0` and the literal forms are named as the boundary and belong to laws 30/31/33</em> | proved-tied | Rchain.lexemes, Rchain.lexemes_decide, Rchain.longestMatchIn | - | lex | rholang/src/parser.rs, node/tests/lean_lex_corpus.rs | the `decide`d `lexemes_decide` fails if two spellings collide or if a row's spelling is not its own longest match — a table where `<` shadowed `<=` breaks the `<=` row; the Rust consumer (`node/tests/lean_lex_corpus.rs`) runs each sample through the real lexer, so a source that drifted from the model fails instead of being trusted |
| **33** | `parse (print p) ≡ p` on `Par` (the C13 round-trip) <br/> <em>needs `Rchain/Print.lean`, which does not exist</em> | open | - | - | - | - | *owed* |
| **34** | Normalization is a function; a *value* position (operand, condition, target, datum, element, pattern, name) is normalized against an empty `par` — only a statement continuation inherits <br/> <em>**the highest-value open law**, and the model cannot yet state it: `normalizeAt` threads only the binder stack `Γ` and takes no accumulated `par`, so the very parameter whose misuse was C21 does not exist here. The defect lived in the Rust normalizer seeding a *value* position with the par that preceded it; a model with no accumulator cannot express that, so the law needs the accumulator modelled (or, sharper and cheaper, a corpus layer carrying the C21 repro — the mechanism that would actually tie it to the node)</em> | open | Rchain.normalizeAt, Rchain.Surf | - | - | - | this is AUDIT C21: with the rule violated, `x!(["a"]) \| if (1 == 1) { … }` produces nothing at all — the defect was observed on a node and the Rust fix is `f6477eba3`, so the negative case is not hypothetical, it is history |
| **35** | `connective_used` is sound: it holds iff the term contains a connective, free variable, wildcard or remainder, per collection form <br/> <em>the layer whose first line — `connective_used` short-circuiting to structural equality — is where C19/C20/C22 item 3 all lived</em> | proved-tied | Rchain.connectiveUsed, Rchain.Var.isConnective, Rchain.cmpOptionVar | - | flags | rholang/src/normalizer.rs, rholang/tests/lean_normalize_corpus.rs | `flagCases_decide` (`Rchain/Corpus.lean`, 17 cases) is `decide`d, so a model whose flag disagreed with its own case would not compile; `rholang/tests/lean_normalize_corpus.rs` then reads each pattern's verdict out of the real node. This layer found AUDIT C24 |
| **36** | The normalizer's output is well-scoped and closed (Law 6 through every path) <br/> <em>reuses `TotalOn`/`Refined` (`Rchain/Ty.lean`)</em> | open | - | - | - | - | *owed* |
| **37** | Match soundness and completeness (Law 5 strengthened: partial collections, wildcards, remainders) <br/> <em>shares its two axioms with Law 5</em> | owed | Rchain.spatialMatchCore, Rchain.spatialMatchExprs, Rchain.spatialMatchExpr, Rchain.matchListPar, Rchain.matchMap | Rchain.concrete_matches_iff_eq, Rchain.fuel_saturation | match | - | 15 cases with three-valued verdicts; the once-false law-5 axiom was replaced *because* a corpus case contradicted it (AUDIT C26), and the fuel bound was one step short until the `decide` refused to compile — the mechanism caught both |
| **38** | Silence is specified: an unmatched receive or `match` yields no reduction **and no error** <br/> <em>the rule is the law; the tie from the rule to the search is owed and named. It was *false* as first stated — `takesStep p = true ↔ ∃ q', ReduceP p q'` for every `p`, refuted by `chan = nilPar` (AUDIT C40) — so it is now domain-restricted to `allStringChans p`, with the sound direction split out unconditionally as `takesStep_sound`, which is the half the corpus leans on. A false axiom is worse than an owed proof: anything follows from it</em> | owed | Rchain.ReduceP, Rchain.takesStep, Rchain.receiveParP | Rchain.takesStep_iff_reduces, Rchain.takesStep_sound | silence | - | `ReduceP`'s only datum-consuming rule carries `spatialMatches data pattern` as a hypothesis, so silence is a consequence of the rule; the corpus's 6 cases each run on their own runtime with a control datum, and a case that stepped when it should not would fail |

### RSpace

| Law | Invariant | Status | Lean | Rests on | Tied by | Models | Falsified by |
|---|---|---|---|---|---|---|---|
| **7** | Join commutativity: channel keys are hashed in sorted order, so the join key is invariant under permutation | deferred | Rchain.joinKey, Rchain.joinKey_perm | Rchain.joinKey, Rchain.joinKey_perm | - | - | *owed* |
| **8** | Deterministic COMM: candidate selection is sorted-first by content hash and produce refs are sorted, so the event trace is content-addressed | deferred | Rchain.produceRefs, Rchain.comm_content_addressed | Rchain.produceRefs, Rchain.comm_content_addressed | - | - | *owed* |
| **9** | Merge is a monoid and non-conflicting logs commute — strengthened for effect scheduling: disjoint **closure** (not footprint) implies commutation <br/> <em>the effect-level strengthening is a theorem; the state-change monoid it is stated over is `deferred` (the four axioms)</em> | proved-model | Rchain.mergeChanges, Rchain.mergeChanges_assoc, Rchain.NonConflicting, Rchain.mergeChanges_comm, Rchain.effect_commute_of_disjoint_closure, Rchain.effect_reorder_diverges | Rchain.mergeChanges, Rchain.mergeChanges_assoc, Rchain.NonConflicting, Rchain.mergeChanges_comm | - | rspace/src/merger/state_change.rs, rspace/src/merger/event_log_merging_logic.rs | `effect_reorder_diverges` is a proved counterexample to the weaker footprint-disjointness reading: there is a published schedule that the naive rule would allow and that changes the result, which is why the law was strengthened to closures |
| **10** | Merkle determinism: the radix trie is content-addressed, collision-free, with a defined empty root | deferred | Rchain.trieRoot, Rchain.trie_collision_free, Rchain.emptyRoot, Rchain.trie_empty_root | Rchain.trieRoot, Rchain.trie_collision_free, Rchain.emptyRoot, Rchain.trie_empty_root | - | - | *owed* |
| **11** | Replay determinism: recomputed COMM is a subset of the recorded trace | deferred | Rchain.replayEvents, Rchain.replay_comm_subset | Rchain.replayEvents, Rchain.replay_comm_subset | - | - | *owed* |

### Rosette

| Law | Invariant | Status | Lean | Rests on | Tied by | Models | Falsified by |
|---|---|---|---|---|---|---|---|
| **12** | Actor atomicity (single-threaded `mbox.nextMsg`) <br/> <em>`rosette`/`roscala` are not wired into the build and the Rust reducer replaces the VM</em> | orphaned | - | - | - | - | *owed* |
| **13** | Reflection: everything is an `Ob`; meta/parent chain; fork-join barrier | orphaned | - | - | - | - | *owed* |

### Casper

| Law | Invariant | Status | Lean | Rests on | Tied by | Models | Falsified by |
|---|---|---|---|---|---|---|---|
| **14a** | Finality requires more than 2/3 of bonded stake, as the exact integer comparison `3·stake > 2·total` (no float rounding) <br/> <em>as stated the axiom restates its own definition (`simp [isSuperMajority, Nat.mul_comm]` closes it) — it ties finality to nothing. Re-scoping it is Task 2's</em> | owed | Rchain.isSuperMajority, Rchain.finality_iff_supermajority | Rchain.finality_iff_supermajority | - | - | the integer form is falsifiable where the float form is not: `3·stake = 2·total` is a boundary case the `>` excludes, and `isSuperMajority` is a `Prop` over `Nat`, so a boundary instance can be checked |
| **14b** | A fringe holds one message per bonded validator (an antichain) | deferred | Rchain.fringe_antichain | Rchain.fringe_antichain | - | - | *owed* |
| **15** | The fringe is monotone by height and the seen-set is monotone (no regression) | deferred | Rchain.fringe_monotone, Rchain.seen_monotone | Rchain.fringe_monotone, Rchain.seen_monotone | - | - | *owed* |
| **16a** | Block number = max(parent) + 1 | deferred | Rchain.Block, Rchain.block_number_max_parent_plus_one | Rchain.block_number_max_parent_plus_one | - | - | *owed* |
| **16b** | `seqNum` strictly increases: the sender's next block is exactly one more than its previous <br/> <em>as stated the axiom quantifies over **any** two blocks (`prev.seqNum + 1 = next.seqNum`), which is false of two unrelated blocks: the sender relation is missing from the statement</em> | deferred | Rchain.seq_num_strictly_increases | Rchain.seq_num_strictly_increases | - | - | *owed* |
| **16c** | Content addressing: `hash = Blake2b256(block − {hash, sig})`, so the hash determines the body <br/> <em>over the model's `hash : Nat` field the axiom is not true — an injective `Nat → Nat` does not exist; a byte-string hash model is needed before this can be more than a postulate</em> | deferred | Rchain.content_addressing | Rchain.content_addressing | - | - | *owed* |
| **16d** | The bonds cache equals the PoS state <br/> <em>no Lean declaration; the Rust side is `BTreeMap<S, NonNegI64>` bonds</em> | open | - | - | - | - | *owed* |
| **17a** | Merge determinism: a rejection resolves to a unique minimum-cost candidate <br/> <em>no Lean statement, and the Rust suggests why: the merge takes the *branch set* (`casper/src/merging.rs`'s `compute_merged_state` over the conflict predicate at `rspace/src/merger/event_log_merging_logic.rs:100-158`) — it does not choose among candidates, so a claim about a unique minimum-cost candidate has nothing in the code to be stated against. What the code does have is Law 9's non-conflict condition, which is modelled there</em> | open | - | - | - | - | *owed* |
| **17b** | The merge's arithmetic is the checked 64-bit one — a value that would leave `i64` is **refused, not wrapped** — and the merged RNG is a function of the *set* of branch generators <br/> <em>**the law that stood here was false**: `numeric_channels_nonneg` claimed numeric channels are non-negative, and they are signed `i64` with ordinary negative diffs (`rholang/src/merging.rs:161-166`, tests at `:349,370` with `diff: -5`). The non-negativity that *is* true belongs to Law 14's bonds and to `NonNegI64` (`shared/src/refined.rs:64`), which types bonds and heights, never numeric channels. **And the arithmetic is only half checked**: the merge result uses `checked_add` (`merging.rs:102`) while the diff accumulator uses a plain `i64 +=` (`rspace/src/merger/event_log_index.rs:151`, `casper/src/merging.rs:758`) — a debug panic, a release wrap. That half is a code finding, recorded as AUDIT §17 C41, not a law</em> | proved-model | Rchain.checkedAdd, Rchain.checkedSub, Rchain.mergeRandoms, Rchain.checkedAdd_refuses_overflow, Rchain.checkedSub_refuses_overflow, Rchain.merge_diff_round_trip, Rchain.mergeRandoms_perm | - | - | rholang/src/merging.rs, rspace/src/merger/event_log_index.rs | the refusal is a witness rather than a remark: `checkedAdd i64Max 1 = none` and `checkedAdd i64Min (-1) = none` (`checkedAdd_refuses_overflow`), which a `checkedAdd` that wrapped would fail; `mergeRandoms_perm` is the statement that the merged RNG does not depend on the order branches arrived in, false the moment the caller's sort is removed |

### Storage

| Law | Invariant | Status | Lean | Rests on | Tied by | Models | Falsified by |
|---|---|---|---|---|---|---|---|
| **18a** | The height map is contiguous: no holes in block heights <br/> <em>as stated it claims a property of *any* `List Block`, which is false of an arbitrary list — the DAG/fringe structure it depends on is not a hypothesis</em> | deferred | Rchain.height_map_contiguous | Rchain.height_map_contiguous | - | - | *owed* |
| **18b** | The fringe identity is order-independent (a set, not a list) <br/> <em>`Perm → f = g` needs `messages` sorted and deduplicated as an invariant of `Fringe`; without it the axiom is false of a `Fringe` built by hand</em> | deferred | Rchain.fringe_identity_order_independent | Rchain.fringe_identity_order_independent | - | - | *owed* |

### Crypto

| Law | Invariant | Status | Lean | Rests on | Tied by | Models | Falsified by |
|---|---|---|---|---|---|---|---|
| **19** | Blake2b256 is canonical and collision-free; the `Blake2b512Random` merge is n-ary and **order-sensitive**; signatures verify what they sign; Curve25519 round-trips <br/> <em>the only `axiomByDesign` law, and where a **false axiom** was found: `mergeRandom_comm` claimed the RNG merge commutes and the code's own test refutes it (`crypto/src/hash/blake2b512_random.rs:548`, `merge_is_order_sensitive`). The merge is modelled n-ary (`List Random → Random`) because that is its Rust signature, and *no* algebraic law of it is claimed — the code has none to claim, so `mergeRandom_assoc` went with the commutativity. Law 17's RNG clause duplicated this one and is merged into it</em> | axiom-by-design | Rchain.blake2b256, Rchain.blake2b256_collision_free, Rchain.sign, Rchain.verify, Rchain.sign_verify_roundtrip, Rchain.sharedSecret, Rchain.curve25519_roundtrip, Rchain.mergeRandom | Rchain.blake2b256, Rchain.blake2b256_collision_free, Rchain.sign, Rchain.verify, Rchain.sign_verify_roundtrip, Rchain.sharedSecret, Rchain.curve25519_roundtrip, Rchain.mergeRandom | - | crypto/src/hash/blake2b256_hash.rs, crypto/src/hash/blake2b512_random.rs | the Rust side is pinned by known-answer tests rather than by these axioms: `crypto/`'s `Blake2b512Random`/`Secp256k1`/`Curve25519` vectors are what would catch a wrong primitive, so the axioms are a *boundary* of the model, not a claim the model establishes |

### Scheduler

| Law | Invariant | Status | Lean | Rests on | Tied by | Models | Falsified by |
|---|---|---|---|---|---|---|---|
| **20** | Channel-task linearization ("1 channel = 1 logical task"): same-channel ops commit in DFS path order through a per-channel claim queue, and the path-smallest pending claim is always committable <br/> <em>ordering proved; the bakery argument for liveness is the axiom</em> | owed | Rchain.queue_commit_path_ordered, Rchain.law20_deadlock_freedom | Rchain.law20_deadlock_freedom | - | - | `queue_commit_path_ordered` is proved, so the ordering half is falsifiable by construction; the deadlock-freedom half is an axiom with no published witness — a queue configuration that never commits would be the counterexample, and none is exhibited |
| **21** | DFS-gate linearization: the gate scheduler refines sequential `Effect.apply`; one-hop next-step pruning is unsound <br/> <em>the strongest-status law in the catalog: a refinement proof plus a disproof of the competing design, both machine-checked</em> | proved-model | Rchain.gate_exec_refines_apply, Rchain.one_hop_depth2_diverges | - | - | rholang/src/reduce.rs, rholang/src/scheduler.rs | `one_hop_depth2_diverges` is a proved depth-2 counterexample to the pruning rule the law forbids — this is the catalog's model of what a falsifiable law looks like: the law and the disproof of its tempting weakening are published together |
| **22** | Next-step closure is computable at dispatch (the matched datum is concrete); computability does not make cross-channel pruning sound <br/> <em>`next_step_closure_computable` itself is `rfl` — it restates the definition's unfolding, so as a *law* it is currently unfalsifiable even though the claim beside it is not</em> | proved-model | Rchain.next_step_closure_computable, Rchain.depth2_next_step_disjoint | - | - | rholang/src/reduce.rs | `depth2_next_step_disjoint` bounds the law: the closure is computable yet cross-channel pruning by it is still unsound, and that is proved rather than asserted |
| **23** | Read-determinism: an effect's chosen candidate, commit outcome and event trace are a deterministic function of the state it reads <br/> <em>the property test `law23_read_state_determines_outcome` (`rspace/src/property_tests.rs`) names it on the Rust side (commit 35dd13b62)</em> | proved-model | Rchain.read_state_determines_outcome | - | - | rspace/src/space_matcher.rs, rspace/src/rspace.rs | stated as an implication from two states agreeing on the effect's closure, so it is falsifiable by a state pair that agrees on the closure yet yields different traces — the depth-2 stale read of Law 24 is exactly the shape that would produce one |
| **24** | DFS-order serializability: a concurrent execution is sound for the block path iff every commit read exactly the state the DFS-earlier effects produced (the versioned write-record layer) <br/> <em>`dfs_serializable_implies_log_equal` does not use its `DFSSerializable` hypothesis — `set_option linter.unusedVariables false` (`SchedulerOnchain.lean:1205`) hides that, and the theorem holds of any dispatched, path-nodup, pinned run. Either use the hypothesis or restate what is proved — Task 2's</em> | proved-model | Rchain.prefixVisible, Rchain.ValidCommit, Rchain.DFSSerializable, Rchain.s3_pair_fails_validation, Rchain.serializable_writer_chain, Rchain.dfs_serializable_implies_log_equal, Rchain.certificate_blind_late_writer_diverges | - | - | rspace/src/concurrent/channel_queue.rs, casper/src/runtime_manager.rs | the certificate's blind spot is *published* as a theorem (`certificate_blind_late_writer_diverges`): a late-writer run passes the certificate and still diverges, which is why the oracle backstop stays load-bearing |
| **25** | Validated speculation: commits may reorder iff each validates Law 24; invalidated runs fall back to the whole-run gate re-run, so the published state is the sequential fold's <br/> <em>`spec/INVENTORY.md` says 'no axioms remain', which is true of the axioms and not of the claim: two of these three statements are true by construction. Task 2's</em> | owed | Rchain.validated_speculation_refines_apply, Rchain.gate_replay_terminates, Rchain.fallback_rerun_published, Rchain.Published | - | - | - | `fallback_rerun_published` proves the fallback reaches the gate fold for `Published` runs; `validated_speculation_refines_apply` is the disjunction `oracleClean run ∨ gateRerun (pathSortedRun run) = gateFold run`, whose second disjunct is proved for **any** run — so as stated the refinement holds without the validation premise, and the law is weaker than its name: `gate_replay_terminates` is `⟨gateRerun ops st, rfl⟩` |

### Cross-shard

| Law | Invariant | Status | Lean | Rests on | Tied by | Models | Falsified by |
|---|---|---|---|---|---|---|---|
| **26a** | A deploy/block's effects bind to exactly one shard | deferred | Rchain.shard_scope_deterministic | Rchain.shard_scope_deterministic | - | - | *owed* |
| **26b** | The shard id is a validated, ordered value <br/> <em>no Lean declaration; the Rust side is the `ShardId` newtype with its parent-shard-id hierarchy (`shared/src/refined.rs`)</em> | open | - | - | - | - | *owed* |
| **26c** | The RNG seed and unforgeable names are shard-scoped | open | - | - | - | - | *owed* |
| **27** | Cross-shard atomicity (2PC): a transaction commits on every participant or aborts on every one — no run leaves a strict subset committed | deferred | Rchain.txn_atomic | Rchain.txn_atomic | - | - | *owed* |
| **28** | Leg idempotency: `prepare`/`commit`/`abort` are idempotent under the transaction id <br/> <em>provable by construction — `applyEffect` is a pointwise update (`if s = l.shard then l.effect else st s`), so the law follows by `funext`; it is an axiom only because nobody wrote the one-line proof. Task 2's</em> | owed | Rchain.applyEffect, Rchain.leg_idempotent | Rchain.leg_idempotent | - | - | *owed* |
| **29** | Decision durability and record determinism: the coordinator's decision is a durable, content-addressed record; a prepared participant can always recover it | deferred | Rchain.commit_record_deterministic | Rchain.commit_record_deterministic | - | - | *owed* |

### Protocol

| Law | Invariant | Status | Lean | Rests on | Tied by | Models | Falsified by |
|---|---|---|---|---|---|---|---|
| **39** | Every `rho:*` urn's reply arity and shape equals its `spec/API-SCHEMA.md` row <br/> <em>checked for the rows the surface language can spell; `ByteArray`-taking urns (crypto, `deployerId:ops`, `authToken:ops`) have no grammar literal and stay pinned by hand-written probes</em> | proved-tied | Rchain.replyCatalog, Rchain.replyCatalog_decide | - | protocol | rholang/src/system_processes.rs, spec/API-SCHEMA.md | `replyCatalog_decide` requires namespaced unique urns, the reply kind agreeing with its slots, and the declared arity agreeing with the arguments as written; the gate additionally requires a `spec/API-SCHEMA.md` row per catalog urn, so a urn without a documented shape fails |
| **40** | Every call in the protocol catalog has an accepting receive at the target's arity <br/> <em>shares the silence layer with Law 38, which the gate documents as deliberate</em> | proved-tied | Rchain.stepsInBinds, Rchain.receiveParPs | - | silence | rholang/src/system_processes.rs | case 10 is `write!(key, value)` against a three-argument `write` — a call at the wrong arity has no step *by the rule*, so the corpus's arity-varying cases are the negative cases, and this is AUDIT C22 item 2's check |
| **41** | Channel balance: a replicable reader restores what it consumes <br/> <em>the static walk over the vendored text was retired unshipped rather than committed with an exception list (AUDIT §17 C22 item 1)</em> | proved-tied | Rchain.replicatedRead, Rchain.readStore, Rchain.storeSurvives | - | store | rspace/src/rspace.rs, casper/tests/genesis_registry.rs | `storeCases_decide` (5 cases) plus `casper/tests/genesis_registry.rs`'s `a_read_does_not_destroy_the_inbox` on a real chain: a read that consumed its datum would fail both, and C25 (`Group`'s `new` reading a dictionary nobody writes) was found by the second |

### JSON

| Law | Invariant | Status | Lean | Rests on | Tied by | Models | Falsified by |
|---|---|---|---|---|---|---|---|
| **42** | `rho_expr_to_par (expr_from_par p) = p`, and the `0 → absent`, `1 → unwrapped`, `n → ExprPar` envelope rule <br/> <em>the unforgeable leaf is outside the model's domain (its `GUnforgeable` carries a level, not the wire bytes) and stays pinned by `rho_expr.rs`'s unit tests</em> | owed | Rchain.parToJE, Rchain.jeToPar, Rchain.render, Rchain.flatPar | Rchain.decode_encode | json | - | `jsonCases_decide` (12 cases) is `decide`d, and `node/tests/lean_json_corpus.rs` runs each case through the node's own `expr_from_par` *and* its `rho_expr_to_par` round-trip, so an envelope rule that was wrong for `n = 2` fails on a case |
| **43** | Each endpoint's serialized shape equals the schema's (camelCase fields, `[]` semantics, error precedence) <br/> <em>checked for the envelope's keys and tags; the types behind them and the document's coverage are named as the boundary (AUDIT C29 fixed the two rows the check found stale)</em> | proved-tied | Rchain.envelopeCatalog, Rchain.envelopeCatalog_decide | - | envelope | node/src/api/grpc/tonic.rs, node/src/api/dto.rs | `envelopeCatalog_decide`: no key contains an underscore (C16's rule), keys distinct, union tags capitalized, names unique; `node/tests/lean_envelope_corpus.rs` holds *both* parties to the catalog — the DTOs' serialization and the served `OPENAPI_JSON` document — so a row that no longer matches either one fails |
