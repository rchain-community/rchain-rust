# Laws 30–43: the surface the matcher reads

[The 29 laws](the-29-laws.md) are about the calculus. The ten defects that started the formalisation
programme — `spec/AUDIT.md` C9–C22 — were not: they were in the *surface* a client writes and a
matcher reads — the grammar, the normalizer, the matcher's patterns, the reply shapes, the JSON. In
every one of them **nothing errored**: an unmatched `for` is not an error, a wrong reply shape is not
an error, a swapped connective is not an error. Each read as a client bug, and each was found on a
running node.

So the catalogue gained fourteen rows, `spec/INVENTORY.md` 30–43, covering that surface. They close
the same way the first 29 do — a Lean declaration, a corpus emitted from it, a Rust consumer held to
the same cases — with one addition that matters: the gate
([`tools/check-lean-conformance.sh`](https://github.com/rchain-community/rchain-rust/blob/dev/tools/check-lean-conformance.sh))
refuses a stale corpus, an unimported module, a `sorry`, and a corpus with no consumer, so a law that
stops holding is noticed by a machine rather than by a user.

| # | Law | Where it lives | State |
|---|---|---|---|
| 30 | every term the parser accepts is in the grammar | `Rchain/Surface.lean`'s production table | the *grammar* is modelled and its productions are witnessed both ways; the **parser** is not modelled, so soundness is open (the gate already reserves a `parse` consumer with nothing behind it) |
| 31 | every grammar term is accepted, modulo a data list of deviations | the same, plus `parser.rs`'s deviation rows | open (the deviation list is not yet a table) |
| 32 | each spelling lexes one way | `Rchain/Lex.lean` (`lexemes`, maximal munch) | **checked** for the operator surface |
| 33 | `parse (print p) ≡ p` | *(pending)* a printer — `Rchain/Surface.lean`'s `Surf` is the type it would print | open: neither the parser nor the printer is modelled |
| 34 | a value position is normalized against an empty par; only a statement continuation inherits | `Surface.lean`'s `normalizeAt` + the `c21` corpus layer (`Corpus.lean`'s `c21Cases`) | **checked on the shapes C21 broke** (5 cases, `rholang/tests/lean_c21_corpus.rs`) — the universal form needs the accumulator modelled, which the model deliberately does not have |
| 35 | concreteness is sound (connective, free var, wildcard **or remainder**) | `Rchain/Par.lean`'s `connectiveUsed` | **checked** (found C24) |
| 36 | the normalizer's output is well-scoped and closed | `Surface.lean`'s `normalize` | modelled; the corpus is open |
| 37 | matching is sound and complete, over the shapes the clauses cover | `Rchain/Match.lean` | **owed** — tied by `match.tsv` (19 cases), but the law's own theorem (`concrete_matches_iff_eq`) is an axiom. Its domain was too wide until C44: a tuple pattern, and any arithmetic pattern, is concrete and unmatchable, so both sides now carry `modelledPar` |
| 38 | silence is specified: no reduction, no error | `Rchain/Silence.lean`'s `ReduceP` | **checked** — the rule is the law and the corpus (13 cases) ties it, and the tie between the rule and the search is a **theorem** in both directions (`takesStep_sound`, `takesStep_complete`, so `takesStep_iff_reduces` is not an axiom). Its statement was false as first written (C40) and then carried a domain hypothesis that the rule's own channel hypotheses made unnecessary. What is left is the model's boundary rather than a proof debt: it has no join rule, so a join with one channel filled is silent here and in the node, while a fully matched join is a step in the node and none here (C40, C45) |
| 39 | every `rho:*` urn's reply shape equals its `API-SCHEMA.md` row | `Rchain/Protocol.lean`'s `replyCatalog` | **checked** for the rows the surface can spell |
| 40 | every call has an accepting receive at the target's arity | `Silence.lean`'s `stepsInBinds` + `ReduceP.commPs` | **checked** (C22 item 2 — and `commPs` is the rule clause that makes it a rule) |
| 41 | a replicable reader restores what it consumes | `Rchain/Store.lean` | **checked** (C22 item 1; C25 measured on the live contract) |
| 42 | the rho-value round-trip, and the envelope rule | `Rchain/Json.lean` | **checked** — the corpus ties the wire form (and C38 fixed that form itself) *and* `decode_encode` is now a theorem: the axiom it replaced was not merely owed but **false**, because its domain predicate admitted a par of two unforgeables, which the decoder drops and the encoder writes as nothing at all (`unforgPair_refutes_the_old_statement`; the predicate now refuses one). C28 is the prerequisite the row rests on: the model's `Ground` had to gain `uri` and `bytes` before the law could be stated at all |
| 43 | each endpoint's serialized shape equals the schema's | `Rchain/Envelope.lean`'s catalog | **checked** for the envelope's keys and tags |

> **The checked version of this table is [`spec/LAWS.md`](../../../spec/LAWS.md)**, emitted from
> `Rchain/Laws.lean` and refused stale by the gate. It counts *entries* rather than laws
> (<!-- counts:laws-entries -->49 laws and 58 entries<!-- counts:end --> today) and distinguishes **`proved-tied`** (proved *and* tied to the node by a conformance corpus)
> from **`proved-model`** (a claim about a model a human keeps in sync) — a distinction this page's
> plainer "checked" used to hide, which is the same gap C21 shipped through. Its honest total is
> **<!-- counts:proved-laws -->37<!-- counts:end --> of <!-- counts:laws -->49 laws<!-- counts:end --> proved at all** — <!-- counts:proved-tied-laws -->9<!-- counts:end --> tied to a corpus, <!-- counts:proved-model-laws -->28<!-- counts:end --> over the model — with one more
> law proved but *vacuous*; the rest are owed, deferred, open, axiomatized by design, or orphaned. Where
> the two disagree, the register is right.

## Two things this table is deliberately not

**It is not a list of proofs.** One row here carries an owed theorem rather than a proved one — law 37,
whose tie is owed for a **singleton** pattern, the shapes its clauses cover (the axiom it replaced was
false twice over and is deleted; AUDIT C51). The rest of this page's proof debt has been *discharged*, not
narrowed: `Match.fuel_saturation` and `Silence.takesStep_iff_reduces` are both theorems now, and the
matcher's former axiom `concrete_matches_iff_eq` went with the over-wide statement it made. Law 38 is the
instructive pair to that one — its statement was false as first written (`chan = nilPar`; AUDIT C40), then
scoped by a domain hypothesis, and the hypothesis itself became unnecessary once the rule's own channel
hypotheses carried that domain (C45), so the tie is stated for every `Par` and proved in both directions.
Law 42's round trip is the other shape of discharge, the one where the *statement* had to be corrected
first: its domain predicate admitted a par of two unforgeables, which decodes to `Nil` and encodes to
nothing. No row on this page rests on an axiom any more; the register's axiom set is law 19's
cryptographic boundary, which is a decision rather than a debt.

One row (36) is *modelled but not tied*: `Surface.lean` defines `normalize`, and the corpus that would
hold it to the node does not exist. Four (30, 31, 33, 36) are `open`, and each says which kind of gap it
is — a missing parser, a missing printer, a missing corpus — rather than implying coverage.

*(Counts in this page are the register's: the numbers above are generated from `spec/laws.tsv` by
`tools/emit-lean-counts.sh` and refused stale by the gate, so a paragraph here cannot carry a total the
register disagrees with. This paragraph itself was corrected on 2026-09-23 — it had claimed a
twenty-five-law corpus total for a catalog that was already a different size, listed law 34 as merely
modelled when its `c21` corpus is tied, and carried the law-42 sentence twice.)*

**It is not a claim that the surface is finished.** Laws 30, 31 and 33 need a Lean lexer, parser and
printer — the largest single lift in the catalogue, and the reason rows 30/31/33 are still open while
32 (the operator table) is checked: a table of spellings can be checked without a parser; a parser
cannot.

## What each layer buys, in one line each

- **Grammar (30, 31, 32, 33)** — a term is what the grammar derives, and nothing else. C30's finding
  is the shape of it: `parse` returned `Ok` for a valid *prefix* (`null )` parsed), and because
  `source_to_adt_with_env` runs on `deploy.data.term`, a deploy could execute part of what a client
  wrote and report success.
- **Normalization (34, 36)** — a value position is normalized against an *empty* par; only a statement
  continuation inherits the accumulated one. C21 lived here: a non-first `if` reduced to nothing.
- **Matching (35, 37, 38)** — an unmatched receive performs no step and reports no error. That is the
  law, stated, and it is why the defects were expensive: the silence *is* the correct behaviour, so a
  broken pattern is indistinguishable from a pattern that legitimately matched nothing.
- **Replies and protocols (39, 40, 41)** — a reply's arity and shape are a row in a table, and a call
  at the wrong arity has no accepting receive *by the rule* (C22 item 2).
- **JSON (42, 43)** — the wire form is the reference document's, arm for arm, and the served document
  is held to the same catalog the DTOs are. C38 is the finding: the code and the law agreed with each
  other and neither with a client, because the law had been written from the code. Its sibling C39 is
  the same class one layer out — the *reply channel* rule, which an exploratory deploy's response now
  states (`replySource`) instead of leaving an empty `expr` to be guessed at.

The last of those is the one worth carrying forward: **a law written from the code is a mirror.** The
corpus cannot catch a wrong shape when the model and the node were derived from each other; only the
contract can, and it has to be read rather than assumed.
